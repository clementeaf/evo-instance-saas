# Evolution API v1.7.4 - Fly.io Deployment
FROM atendai/evolution-api:v1.7.4

# Fly.io uses PORT env variable
ENV SERVER_URL=https://evolution-api.fly.dev \
    PORT=8080

# The rest of config will come from fly.toml and secrets
